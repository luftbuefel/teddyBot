import pyttsx3
import happy
import speech_recognition as listen

talker

talker.say("Hey Charles, how is it going?")
talker.runAndWait()


def setup():
    global talker
    talker = pyttsx3.init()

def forget():
    memory = None
    saveMemory()

def remember:

def saveMemory():
    if memory == None:
        memory = {}
    #save the memory into a local file or database

def askQuestion():
    #keys =
    #if len(keys)>0:
        #currentQuestion = random(keys)
        
def analyzeSpeech(phrase):
    if len(phrase)>0:

def say(phrase):
    talker.say(phrase)
    for attribute in range():

def respondToSpeech(phrase):
